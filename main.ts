let strip = neopixel.create(DigitalPin.P15, 4, NeoPixelMode.RGB)
let led00 = strip.range(0, 1)
let led01 = strip.range(1, 1)
let led02 = strip.range(2, 1)
let led03 = strip.range(3, 1)
led00.showColor(neopixel.rgb(255, 0, 0))
led01.showColor(neopixel.rgb(255, 255, 0))
led02.showColor(neopixel.rgb(0, 0, 255))
led03.showColor(neopixel.rgb(0, 255, 0))
